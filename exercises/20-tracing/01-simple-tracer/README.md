# Simple tracer / instrumentation helper

Implement a tiny tracer that can start/end spans and report durations. This is intentionally minimal — a learning step toward full tracing systems.

Run: `node test.js`
